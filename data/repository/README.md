# Repository

Implementation of the repository interfaces defined in the **Domain** layer. It orchestrates the data flow between different data sources (Local and DataStore) and maps them to domain models.
